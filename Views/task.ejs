<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/style.css">
    <title>Our Home</title>
</head>
<body>
    <div class="navbar-fixed">
        <nav>
          <div class="nav-wrapper">
            <!-- Add login UI here -->
            <ul class="right">
              <li>
              <% if (task) { %>
                <a href="/logout"><i class="material-icons right">trending_flat</i>Log Out</a>
              <% } else { %>
                <a href="/auth/google"><i class="material-icons right">vpn_key</i>Login with Google</a>
              <% } %>
              </li>
            </ul>
          </div>
        </nav>
    <div class="container">
        <h2> Could You Please......</h2>

        <form action="/add" method="POST">
            <input type="text" name="createTask" placeholder="What shall I do?">
            
            <button>ADD</button>
           
            <h2>Task List</h2>
            
            
        <% for( var i = 0; i < task.length; i++){ %>
         <li><input type="checkbox" name="check" value="<%= task[i] %>" /> <%= task[i] %> </li>
        <% } %>

        <button formaction="/removetask" type="submit" id="top"> Remove </button>

        </form>

        <h3>Cookie Jar</h3>

        <% for(var i = 0; i < complete.length; i++){ %>
            <li><input type="checkbox" checked><%= complete[i] %> </li>
        <% } %>
    </div>
    
</body>
</html>
